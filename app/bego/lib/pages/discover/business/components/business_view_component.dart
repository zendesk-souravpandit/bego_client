import 'package:flutter/material.dart';

class BusinessViewComponent extends StatelessWidget {
  const BusinessViewComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Business View'));
  }
}
