import 'package:flutter/material.dart';

class EditItemComponent extends StatelessWidget {
  const EditItemComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Edit Item'));
  }
}
