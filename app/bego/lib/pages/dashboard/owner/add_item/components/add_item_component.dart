import 'package:flutter/material.dart';

class AddItemComponent extends StatelessWidget {
  const AddItemComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Add Item'));
  }
}
