import 'package:flutter/material.dart';

class BusinessOwnerComponent extends StatelessWidget {
  const BusinessOwnerComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Business Owner'));
  }
}
