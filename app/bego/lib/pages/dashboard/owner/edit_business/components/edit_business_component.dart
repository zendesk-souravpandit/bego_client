import 'package:flutter/material.dart';

class EditBusinessComponent extends StatelessWidget {
  const EditBusinessComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Edit Business'));
  }
}
