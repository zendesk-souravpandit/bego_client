import 'package:flutter/material.dart';

class CatalogListComponent extends StatelessWidget {
  const CatalogListComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Catalog List'));
  }
}
