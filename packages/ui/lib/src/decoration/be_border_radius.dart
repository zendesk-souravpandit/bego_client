import 'package:flutter/material.dart';

// this.xs = 12.0,
// this.sm = 16.0,
// this.md = 20.0,
// this.lg = 24.0,
// this.xl = 28.0,
// this.xl2 = 32.0,

const br0 = BorderRadius.all(Radius.zero);
const br4 = BorderRadius.all(Radius.elliptical(4, 4));
const br8 = BorderRadius.all(Radius.elliptical(8, 8));
const br12 = BorderRadius.all(Radius.elliptical(12, 12));
const br16 = BorderRadius.all(Radius.elliptical(16, 16));
const br20 = BorderRadius.all(Radius.elliptical(20, 20));
const br24 = BorderRadius.all(Radius.elliptical(24, 24));
const br28 = BorderRadius.all(Radius.elliptical(28, 28));
const br32 = BorderRadius.all(Radius.elliptical(32, 32));
